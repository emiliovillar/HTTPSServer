// Test JavaScript file for HTTP server
console.log("HTTP Server JavaScript test loaded!");

function showAlert() {
    alert("Hello from the HTTP server!");
}

// Add some dynamic content
document.addEventListener('DOMContentLoaded', function() {
    const body = document.body;
    if (body) {
        body.style.backgroundColor = '#e8f4f8';
    }
});
